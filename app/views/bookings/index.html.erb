<p style="color: green"><%= notice %></p>

<h1>Bookings</h1>

<div id="bookings" class="bookings-index">
  <% @bookings.each do |booking| %>
    <%= render booking %>

  <% end %>
</div>

<%= link_to "New booking", new_booking_path %>
